# QA 연구 노트

> 테스트 개선 아이디어, 실험, 학습 기록

---

## 진행 중인 연구

### 🔬 [연구 주제 템플릿]

**배경**: 왜 이것을 연구하는가?

**목표**: 무엇을 달성하려는가?

**접근법**:
1. 단계 1
2. 단계 2

**결과**: (진행 중)

**결론**: (예정)

---

## 완료된 연구

### ✅ Playwright vs Cypress 비교 (2025-01)

**배경**: E2E 테스트 도구 선정 필요

**목표**: 우리 환경에 적합한 도구 선택

**비교 결과**:

| 항목 | Playwright | Cypress |
|------|------------|---------|
| 브라우저 | 모든 브라우저 | 주로 Chrome |
| 언어 | TS/JS/Python | JS/TS |
| 속도 | 빠름 (병렬) | 보통 |
| 디버깅 | Trace Viewer | 시간 여행 |
| 모바일 | 지원 | 미지원 |
| 학습 곡선 | 중간 | 낮음 |

**결론**: Playwright 선택
- 다중 브라우저 지원 필요
- 병렬 실행으로 CI 시간 단축
- 모바일 뷰포트 테스트 가능

---

## 아이디어 백로그

### 💡 Visual Regression Testing

**설명**: 스크린샷 비교로 UI 변경 감지

**도구**: `@playwright/test`의 `toHaveScreenshot()`

**시도해볼 것**:
```typescript
await expect(page).toHaveScreenshot('homepage.png');
```

**예상 효과**: CSS 변경으로 인한 레이아웃 깨짐 조기 발견

---

### 💡 API Mocking

**설명**: 외부 API 응답을 모킹하여 테스트 안정화

**도구**: Playwright의 `page.route()`

**시도해볼 것**:
```typescript
await page.route('**/api/products', route => {
  route.fulfill({
    body: JSON.stringify({ products: mockProducts })
  });
});
```

**예상 효과**: 외부 의존성 제거, 테스트 속도 향상

---

### 💡 Performance Testing

**설명**: 페이지 로드 시간 측정

**도구**: Playwright + Lighthouse

**시도해볼 것**:
```typescript
const metrics = await page.evaluate(() =>
  JSON.stringify(window.performance.timing)
);
```

---

### 💡 Accessibility Testing

**설명**: 접근성 자동 검사

**도구**: `@axe-core/playwright`

**시도해볼 것**:
```typescript
import { injectAxe, checkA11y } from '@axe-core/playwright';

await injectAxe(page);
await checkA11y(page);
```

---

## 학습 자료

### 공식 문서

- [Playwright 공식 문서](https://playwright.dev/docs/intro)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Test Generator](https://playwright.dev/docs/codegen)

### 유용한 블로그

- (발견하면 추가)

### 참고할 오픈소스

- (발견하면 추가)

---

## 회고 기록

### 2025-01 회고

**잘된 점**:
-

**개선할 점**:
-

**다음 액션**:
-

---

## 기록 방법

1. **아이디어 발생** → 아이디어 백로그에 추가
2. **연구 시작** → 진행 중인 연구로 이동
3. **연구 완료** → 완료된 연구로 이동, 결론 정리
4. **월말** → 회고 기록 추가
